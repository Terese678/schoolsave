# Dependencies, reinstalled from package.json, never committed.
node_modules

# Hardhat build outputs, regenerated on every compile.
artifacts
cache

.env

# Environment variables and secrets, never committed.
.env